# Kubernetes ServiceAccount 配置（配置 IRSA）
# 作者: RJ.Wang
# 邮箱: wangrenjun@gmail.com
# 创建时间: 2025-11-14

apiVersion: v1
kind: ServiceAccount
metadata:
  name: eks-info-app-sa
  namespace: rj-webdemo
  annotations:
    # IRSA 注解 - 需要替换为实际的 IAM Role ARN
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/eks-info-app-role
  labels:
    app: eks-info-app
